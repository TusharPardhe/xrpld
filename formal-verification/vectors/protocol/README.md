# Protocol vectors

Current Protocol vectors are Rust-backed executable constructors under `rust/crates/parity-harness/src/protocol/`, with STAmount fixtures grouped under `protocol/vectors/`. Migrated production regressions live under `rust/tests/protocol/`.

They are intentionally not described as language-neutral. Stable IDs and semantic/boundary/guard classifications are defined in `../schemas/catalog.toml`.
